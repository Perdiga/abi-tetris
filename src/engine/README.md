# Engine workspace

Owned by Mr. Pink.

Planned modules:

- `geometry/` for masks, rotation, and compartment math
- `rules/` for equipment and nesting validation
- `inventory/` for placement and movement logic
- `run/` for the training-mode timer, loot events, and scoring state machine
