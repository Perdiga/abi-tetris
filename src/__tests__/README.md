# Test workspace

Owned by Mr. Blue.

Vitest will pick up either:

- `src/**/*.{test,spec}.ts(x)`
- `src/**/__tests__/**/*.{ts,tsx}`

Place focused tests here or co-locate them beside the module under test.
